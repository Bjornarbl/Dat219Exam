@model IEnumerable<hva_som_skjer.Models.ClubModel>

@{
    var m = Model.ToList();
    ViewData["Title"] = "Søkeresultat";
}

@foreach (var p in Model) 
{
    Html.RenderPartial("_ClubPartial", p);
}    